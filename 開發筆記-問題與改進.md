# 吃什麼大冒險 — 開發筆記（問題 · 解法 · 可改進）

> 記錄於 2026-08-18（爸爸生日當天上線）
> 專案：8-bit 迷宮小遊戲，給爸爸的生日禮物。單一 index.html + PWA。
> 線上：https://pangyi681.github.io/dad-dinner-game/ （私後轉公開，GitHub Pages）

---

## 一、遇到的問題 & 解決方式

### A. 遊戲邏輯 / 引擎
1. **預覽視窗裡遊戲不動、角色沒畫出來**
   - 原因：瀏覽器分頁在背景時會暫停 `requestAnimationFrame`，遊戲迴圈停擺。
   - 解法：加一個 `setInterval` 備援迴圈，只在「rAF 超過 0.2 秒沒更新」時才接手，避免和 rAF 疊加變兩倍速。

2. **遊戲還沒開始就報錯 `Cannot read ... forEach`**
   - 原因：`draw()` 每幀都跑，但 enemies/inspirations 要按「開始」後才初始化。
   - 解法：`draw()` 先畫迷宮，偵測到未初始化就 `return`；把牆的繪製抽成 `drawMaze()`。

3. **舊「轉盤版」指針指到的和顯示結果不一致**
   - 解法：改成迷宮遊戲後這個 bug 自然消失（結果改由收集靈感觸發）。

### B. 操作手感（來回調了好幾輪）
4. **鬼太快、太難** → 放慢 `ENEMY_SPEED`、調低 `CHASE_CHANCE`；並把所有難度參數集中到檔案最上面方便調。
5. **玩家太快不好控** → 放慢 `PLAYER_SPEED`。
6. **方向鍵不直覺、搖桿沒按準會誤判** → 改成「浮動搖桿」：以手指第一次接觸點為中心算相對位移，按左側任何位置都行。
7. **轉角卡住**（格子式移動的經典問題）：預先撥了轉彎、但那格側邊是牆就整個停住。
   - 解法：加「轉向緩衝 + 直行遞補」——想轉的方向暫時是牆時，先沿原方向直行，到第一個能轉的路口自動過彎（小精靈那種手感）。
8. **衝刺不好操作**（加速反而更難控） → 應要求把「衝刺」改成純「無敵」，不加速、好操控。

### C. 美術 / 8-bit 化
9. **中文像素字體**：Press Start 2P 這類只支援英文，中文顯示不出來。
   - 解法：改用 **Cubic 11**（繁體中文像素字，OFL 授權），檔案只有 ~400KB，base64 內嵌進單一檔。
10. **沒有 PIL / node-canvas 可處理圖片**：改用 mac 內建 `sips`（支援 `--cropOffset`、`--padToHeightWidth --padColor` 補白、`--resampleHeightWidth` 縮放）。
11. **像素臉解析度取捨**：太高像小照片、太低不像本人 → 44~60px 平衡。
12. **手繪線稿臉在小迷宮格裡偏暗看不清** → 加白底墊 + 亮白外框，讓臉牌從深色迷宮跳出來。
13. **開始畫面爸爸頭頂被切** → sips 裁切 offsetY 切到髮頂；改「抓完整頭部 + 左右補白置中」。
14. **emoji 有廉價 AI 感** → 全部換成自畫像素 icon：寫一個「grid 字串 → SVG rect」產生器，canvas 內道具也共用同一套。
15. **配色不好看且難改** → 把顏色抽成 CSS 變數 + JS `PALETTES`，做成畫面下方即時切換器（4 色）。

### D. 部署 / GitHub（GitHub 新手向）
16. **`gh` CLI 沒裝**：私人 repo 無法用命令列建立 → 引導在網頁建 repo，再用 SSH 別名 `github-me` 推。
17. **中文 repo 名被 GitHub 吃掉**：「晚餐吃什麼？」被消成單一 `-` → 改英文名 `dad-dinner-game`，並用 `git remote set-url` 更新本機連結。
18. **要不要填 Custom domain**：不用，留空即用免費 `*.github.io` 網址。

### E. 快取（最關鍵的線上坑）
19. **手機一直開到舊版、更新看不到（「串燒沒成功」的真因）**
   - 原因：Service worker 用 **cache-first**，第一次上線的舊版被快取後就一直回舊的，之後的更新（串燒等）在手機上看不到。
   - 解法：改成 **network-first**（連網優先抓最新，離線才用快取）+ 升 cache 版本號清舊快取。
   - 提醒使用者：舊 SW 還在的話，關分頁重開 / 重整兩三次 / 無痕視窗，即可換到新版。

### F. 其他小問題
20. **`navigator.vibrate` 被擋警告**：自動測試（非真實點擊）觸發震動被瀏覽器擋，真機上按按鈕正常，非 bug。
21. **串燒「沒成功」誤會**：其實程式正確（生日當天才抽串燒）+ 快取問題；順手加 `?bday` 預覽開關，方便非生日日測整套生日體驗。

---

## 二、可以做更好的地方（未來）

- **多張迷宮輪替**：目前只有一張，做 3~4 張輪流出更耐玩。
- **PWA 更新提示**：偵測到新版時跳「有更新，點一下刷新」，比叫使用者手動重整友善。
- **食物清單可在畫面內編輯**：讓爸爸自己加減菜色，不用改程式。
- **音效更豐富**：加背景音樂、更多層次的 8-bit 音效。
- **難度隨進度漸強**：收越多雞腿鬼越兇；或做關卡。
- **字型子集化**：Cubic 11 只保留用到的字，可從 400KB 再縮小、載入更快。
- **無障礙**：色盲友善的鬼配色、字級選項。
- **一鍵部署腳本**：目前手動 commit/push，可包成一個指令。

---

## 三、可重用的技術筆記（下次做類似東西直接參考）

- 單一 HTML 遊戲：字型/圖片都能 base64 內嵌，做到「一個檔到處開」。
- rAF 在背景會停 → 需要備援迴圈才能在某些情境穩定跑。
- 格子式移動要好玩，一定要做「轉向緩衝 + 自動過彎」。
- 浮動搖桿 >> 固定方向鍵（觸控直覺度差很多）。
- 中文像素字：Cubic 11 / Zpix（OFL），可 base64 內嵌。
- 沒 PIL 時：mac 的 `sips` 能裁切/補白/縮放；純 Python QR 用 `segno`（無依賴）。
- PWA 給「會一直更新」的專案：務必 network-first，別用 cache-first。
- GitHub repo 名用英文；中文會被消成怪字。
